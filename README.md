# dotclean3ds
### recursively finds and safely removes:
* `._*`
* `.DS_Store`
* `.Trashes` & `.trashes`
* `.fseventsd`
* `.Spotlight-V100`

### build instructions
you need devkitpro and devkitarm in path
open folder in terminal
make
done
